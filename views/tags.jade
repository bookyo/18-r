extends layout

block content
  .container
    .row
      section.tags.clearfix
        each tag in tags
            .col-md-3
              a.tag(href="/tag/#{tag._id}" title="#{tag.tag}电影大全") #{tag.tag}
