extends layout

append title
  title #{title}

block content
  .container
    .row
      .col-md-3
        section.tags.clearfix
          a.tag.current(href="/") 全部
          each tag in tags 
            a.tag(href="/tag/#{tag._id}") #{tag.tag}
      .col-md-9
         section#main-body.clearfix
           include movie